[project-build]

command=go mod tidy && go build

cwd=$(VIM_ROOT)

output=quickfix

errorformat=%f:%l:%m

save=1

[project-run]

command=go mod tidy && go run main.go

cwd=$(VIM_ROOT)

output=terminal

save=1
